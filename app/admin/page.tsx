import { AdminApp } from '@/components/admin/admin-app'

export const metadata = {
  title: 'Administration — Portfolio Emmanuel SINGBE',
  robots: { index: false, follow: false },
}

export default function AdminPage() {
  return <AdminApp />
}
